# ---------------------------------------------------------
# @rebwar_ai Linux
# ---------------------------------------------------------
MCU = atmega2560            # Target microcontroller
F_CPU = 16000000UL          # CPU frequency (16 MHz)
BAUD = 115200               # Baud rate for uploading
PORT = /dev/ttyACM0         # Example serial port on Linux
SRC = blink.c               # Source file
TARGET = blink              # Output file name (without extension)

# ---------------------------------------------------------
# ⚙️ Toolchain Configuration
# ---------------------------------------------------------
CC = avr-gcc
OBJCOPY = avr-objcopy
AVRDUDE = avrdude

# ---------------------------------------------------------
# 🏗️ Compiler Flags
# ---------------------------------------------------------
CFLAGS = -mmcu=$(MCU) -DF_CPU=$(F_CPU) -Os

# ---------------------------------------------------------
# 🎯 Default Target
# ---------------------------------------------------------
all: $(TARGET).hex

# ---------------------------------------------------------
# 🧩 Step 1: Compile and link → ELF file
# ---------------------------------------------------------
$(TARGET).elf: $(SRC)
	$(CC) $(CFLAGS) -o $@ $<

# ---------------------------------------------------------
# 🧩 Step 2: Convert ELF → HEX file
# ---------------------------------------------------------
$(TARGET).hex: $(TARGET).elf
	$(OBJCOPY) -O ihex $< $@

# ---------------------------------------------------------
# 🚀 Upload to Arduino Mega 2560
# ---------------------------------------------------------
upload: $(TARGET).hex
	$(AVRDUDE) -D -p $(MCU) -c wiring -P $(PORT) -b $(BAUD) -U flash:w:$<

# ---------------------------------------------------------
# 🧹 Clean build files (Linux uses "rm")
# ---------------------------------------------------------
clean:
	rm -f $(TARGET).elf $(TARGET).hex
